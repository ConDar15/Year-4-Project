#ifndef EXACT_ROOT_HEADER
#define EXACT_ROOT_HEADER
	#include <inttypes.h>

	static const char *DIGITS = "0123456789";

	char *root_digits_precise(char*, unsigned int);
	uintmax_t uint_sqrt(uintmax_t);
#endif
